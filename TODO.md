# TODO (mywebtools)

## Milestone: v0.0.4 - ポモドーロタイマーの UI 改善

### 概要

「自分で使えるレベル」を目標に、ポモドーロタイマーの実用性を高める。
今回は，UIを設計レベルから見直す。

### コード・機能

- [ ] **Must5**: タイポグラフィヒエラルキーの整理（情報の優先順位の明確化）
  - **背景**: 現状の UI は「とりあえず動くものを作った」段階で、レイアウトに設計意図がない。
      そこでまずは実装前に「このツールで何を最優先に見せたいか」を一度整理すべき。より具体的には，
      現状は「Focus Time!」というフェーズ名と残り時間の数字が同列の視覚的重みで
      表示されており、ユーザーが最初に目を向けるべき情報（残り時間）が際立っていない。
      「視覚的ヒエラルキー」とは、情報の重要度に応じてサイズ・太さ・色の濃さを変え、
      「何を先に読むべきか」を視覚的に伝えるデザインの基本原則。
  - **方針案**:
    - 残り時間（数字）を画面の主役にする。現状より大きく・太く表示する
    - フェーズ名（「Focus Time!」など）は補足情報として、数字より小さく・薄い色で表示する
    - 実装前に紙などでラフスケッチし、「何をどの大きさで見せるか」の設計意図を
        言語化してから実装に入ること（設計なしにコードを書き始めない）

- [ ] **Must6**: ボタンの視覚的役割分担の明確化
  - **背景**: 現状は「Pause」「Skip →」「Reset」の3ボタンがほぼ同じ視覚的重みで並んでおり、
      「主操作」「補助操作」「破壊的操作」の区別が見た目から読み取れない。
      shadcn/ui の `variant` はまさにこの役割分担を表現するための仕組みであり、
      現状はその意図を活かしきれていない。
  - **方針案**:
    - Start/Pause → `default`（最も目立つ。主操作）
    - Skip → → `outline` （現状維持。補助操作として控えめ）
    - Reset → `ghost` または `destructive` を検討する
      （Reset は「進行中の状態を全て破棄する」破壊的操作であるため、
      誤操作を防ぐ意味でも視覚的に「危険寄り」か「最も控えめ」にするのが適切）
    - 上記の方針は実装前のラフスケッチで確認してから決定すること

- [ ] **Must7**: カードレイアウトの見直し
  - **背景**: 現状の白いカードはコンテンツをただ囲んでいるだけで、
      周囲の青い背景との関係性に設計意図が感じられない。余白（padding）も均一すぎる。
      「カードを使うべきか」「使うとしたらどんな役割を持たせるか」を整理する必要がある。
  - **方針案（検討事項）**:
    - 選択肢A: カードを廃止し、背景に直接コンテンツを配置する
      （フェーズ背景色との一体感が出る。シンプルになる）
    - 選択肢B: カードを残しつつ `shadow` を加えて浮き上がり感を出す
      （背景色との対比が明確になる）
    - どちらが適切かは Must5 のラフスケッチ段階で合わせて判断すること
    - カードの幅・余白も、コンテンツ量に対して適切なサイズに調整する

- [ ] **Must8**: フェーズ切り替え時の視覚的フィードバック強化
  - **背景**: 現状は背景色が変わるだけで、フェーズが切り替わったことの「体感」が薄い。
      特に長時間作業中は、気づかずにフェーズをまたいでしまう可能性がある。
      色覚特性のあるユーザーにとっては、背景色だけでは全く区別できない問題もある
      （色に頼らない識別手段を用意することは、アクセシビリティの基本原則）。
  - **方針案**:
    - フェーズ名にアイコンを併用する（例: 🍅 Focus Time / ☕ Break Time）
      色だけに頼らず、テキストとアイコンの組み合わせでフェーズを識別できるようにする
    - フェーズごとにボタンのアクセントカラーも変えることを検討する
      （例: 集中タイム = 青系、休憩タイム = 緑系。ただし shadcn/ui の
      テーマ変数との兼ね合いを確認すること）
    - Must5 のラフスケッチと合わせて全体の色設計を整理してから実装すること

### テスト・CI

テストなし（ADR-007 の方針に従う）。

### ドキュメント

無し。

### プロジェクト管理

無し。

---

## Milestone: v0.0.5 - ポモドーロタイマーの設定機能

### 概要

v0.0.3 で積み残した設定機能を実装する。
設定の永続化（ページリロード後も保持）の設計判断が必要なため、v0.0.3 から分離した。

### コード・機能

- [ ] **Should1**: 設定画面（FOCUS時間・BREAK時間を変更できるようにする）の実装
  - 普段は隠して、設定アイコンなどから開く UI にする（メイン画面をシンプルに保つ）
  - 設定の永続化をどうするか事前に検討・ADR 記録が必要（後述）
  - 設定可能項目については，今後増やすことも検討している
  - ADR を書くこと。案：「`localStorage` 利用の是非について」
    - 設定の永続化手段として `localStorage` が有力候補
    - トレードオフ（永続化の恩恵 vs 実装複雑度・プライバシー考慮）を整理して決定すること
- [ ] **Should2**: キーバインディング（優先度低）
  - 例: Space でスタート/ポーズ、→ で Skip など

### テスト・CI

テストなし（ADR-007 の方針に従う）。

### ドキュメント

無し。

### プロジェクト管理

無し。

---

## Backlog（いつかやる・検討中）

優先度・着手時期は未定。着手が決まったら適切なマイルストーンに移動すること。

### コード・機能

- [ ] ツール拡充（アイデアは末尾のメモ参照）
- [ ] UI/UX 改善
- [ ] Pomodoro Timer の UI について、Carousel（shadcn/ui）の採用を検討する
  - ADR-016 にて Carousel 採用案は否決された。その一番の理由は「内部に Embla Carousel
      を使っており、今回の目的に対して学習・実装コストが見合わない」であった。
  - しかし、サンプル（下記リンク）の操作・見た目の感触が良く、
      フェーズ切り替えにCarouselを使うアイデアは引き続き魅力的に感じている。
  - なので、余裕があれば再度検討したい。
  - Ref: [Carousel - shadcn／ui](https://ui.shadcn.com/docs/components/radix/carousel)

### テスト・CI

- [ ] テストの導入を検討（Vitest + React Testing Library が有力候補）
  - 検討結果は ADR に記録すること

### ドキュメント

- [ ] `README.md` に色々書く

### プロジェクト管理

- [ ] カスタムドメインの取得を検討
- [ ] Cloudflare Pages の導入を検討
  - まずは別プロジェクトで試してから判断する
- [ ] License を追加する（→ ADR #4 参照。ライセンス選定が未定なら先に ADR #4 を書くこと）

---

## 追加ツールのアイデアメモ（未着手・未確定）

アイデアが出たら随時追記。

### 🟢 比較的簡単

- 文字数カウンター — 文字数・単語数・行数をリアルタイム表示
- Base64 エンコーダー/デコーダー — テキストをBase64に変換
- カラーピッカー & 変換ツール — HEX / RGB / HSL の相互変換
- QRコードジェネレーター — テキスト入力 → QRコード表示

### 🟡 中くらい

- カレンダー
- IP アドレス計算
- JSONフォーマッター & バリデーター — JSON整形・エラー表示
- 単位変換ツール — 長さ・重さ・温度など複数カテゴリ
- カラーパレットジェネレーター — 1色から補色・類似色を自動生成
- 画像加工ツール（リサイズ・トリミング）— Canvas API を使用

### 🟡 難しそう

- 正規表現テスター — パターン入力 → マッチ箇所ハイライト
- タイピング練習ツール — WPM・精度を計測
- レスポンシブデザインチェッカー — 複数ビューポートサイズでプレビュー
- マークダウンプレビュアー — 左エディタ・右プレビューの2ペイン構成
- Markdownエディタ（localStorage保存付き）
- タスク管理（ToDoリスト）— CRUD操作の基本
- ブレインストーミングボード（付箋ツール）— ドラッグ可能な付箋
- ピクセルアートエディタ — グリッドにクリックで色を塗る
- モールス信号変換器 — テキスト変換 + Web Audio API で音を鳴らす
- ポモドーロ統計トラッカー — 既存ポモドーロタイマーにセッション記録・グラフ追加
- CSS Flexbox/Grid ビジュアライザー — プロパティを変更して即時プレビュー
