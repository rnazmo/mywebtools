# ADR (mywebtools)

## ADR-017: ポモドーロフェーズごとの背景色変更の実装範囲と手法

- 日付: 2025-06-01
- 状態: 決定済み

### 背景

ポモドーロタイマーの「集中タイム」と「休憩タイム」を視覚的に区別するため、フェーズ切り替え時に背景色を変える機能を追加した。
実装にあたって「どの範囲に色を適用するか」と「Tailwindでどう実装するか」の2点を検討した。

### 検討1: 色を適用する範囲

候補は以下の2案だった。

- **案A: ページ全体（`<body>` または `<Layout>`）**
  - 視覚的インパクトが大きく、状態変化が一目でわかる
  - サイドバー（`bg-gray-800` 固定）やヘッダー（`bg-gray-100` 固定）と色が混在し、見た目が破綻しやすい
  - 他ツールにも影響が及ぶ設計になる
- **案B: `<main>` 以下（Pomodoro コンポーネントの描画領域）**
  - 他ツールに影響しない
  - 実装がシンプル

**案B を採用する。**

「このポモドーロタイマーはあくまでツールの１つ」という方針に沿い、他ツールへの影響を局所化する。
将来「全ツールに統一テーマを適用する」設計が必要になった時点で、改めて案Aを検討する。

### 検討2: Tailwind での動的クラスの扱い

Tailwind はビルド時にソースコードをスキャンして使用クラスを収集し、未使用クラスはCSSから除外する。
そのため、以下のように文字列を動的に組み立てると、スキャンで検出されずスタイルが当たらない。

```tsx
// ❌ ビルド時に検出されない
const bgClass = `bg-${color}-50`;

// ✅ クラス名全体を静的な文字列リテラルとして書く
const bgClass = isFocusTime ? "bg-blue-50" : "bg-green-50";
```

クラス名全体を静的な文字列リテラルで記述することで、この問題を回避する。

### 決定内容

- 適用範囲: Pomodoro コンポーネントの最外ラッパー `<div>` に背景色クラスを適用し、`min-h-screen -m-6 p-6` で `<main>` の `p-6` を打ち消してビューポート全体を覆う
- 色: 集中タイム = `bg-blue-100`（ダーク: `bg-blue-900`）、休憩タイム = `bg-green-100`（ダーク: `bg-green-900`）
- トランジション: `transition-colors duration-700` でなめらかに変化させる
- Tailwindの動的クラスはクラス名全体を静的な文字列リテラルとして記述する

### トレードオフ

- `-m-6 p-6` という「打ち消し」の手法は、`<Layout>` の `p-6` という実装詳細に依存している。Layout の padding が変わった場合は、こちらも合わせて修正が必要になる
- 将来「全ツール統一テーマ」が必要になったときは、Layout 側でテーマ管理する設計へのリファクタリングが必要になる

## ADR-016: 手動フェーズ切り替えのUI設計

- 日付: 2025-06-01
- 状態: 決定済み

### 背景

ポモドーロタイマーにおいて、「集中が早めに終わった」「休憩を切り上げたい」というユースケースに対応するため、手動でフェーズを切り替えるUIが必要になった。

候補として以下の2案を検討した。

- **案A: Skip ボタン**
  - `Skip →` ボタンを1つ追加し、現在のフェーズを終わらせて次へ進む
- **案B: Carousel（shadcn/ui）**
  - shadcn/ui の Carousel コンポーネントを使い、左右操作でフェーズを切り替える
  - キーボードの ← → でも操作できる

### 決定内容

案A（Skip ボタン）を採用する。

### 理由

- ポモドーロは「フォーカス → 休憩 → フォーカス → ...」という一方向の流れであり、「進む」という操作モデルに `Skip →` が自然に合致する
- Carousel は双方向の行き来を想起させるUIであり、ポモドーロのメンタルモデルと噛み合わない
- 既存の `Start/Pause`・`Reset` に1ボタン追加するだけで、対象ユースケースをすべてカバーできる
- shadcn/ui の Carousel は内部に Embla Carousel を使っており、今回の目的に対して学習・実装コストが見合わない

### Skipボタンの仕様

- 動作: 現在のフェーズを終了 → 次のフェーズにリセット → **停止状態**（自動開始しない）
  - 自動開始しない理由: ユーザーが準備できたタイミングで Start する操作モデルを壊さないため
- スタイル: `variant="outline"`（主操作である Start/Pause・Reset より視覚的に控えめにすることで、イレギュラー操作であることを表現する）

### トレードオフ

- Carousel 案と比べるとキーボード操作に対応していない（将来必要になれば `useEffect` でキーイベントを登録することで追加できる）

## ADR-015: コンポーネントのディレクトリ構成について

- 日付: 2025-04-18
- 状態: 決定済み（意思決定なし）

現状のフラット構成（`src/components/` 直下にページコンポーネントとUIパーツを並べる）は、
意図的な設計判断の結果ではなく、当時のスキルレベルで自然にそうなったものである。
不都合が生じた場合や規模が大きくなった場合は、適宜見直すこと。

### 現状の構成に関するメモ

現状のコンポーネント構成：

```txt
src/
  components/
    layout.tsx
    home.tsx
    stopwatch.tsx
    timer.tsx
    pomodoro.tsx
    uuid-v4.tsx
    random-string.tsx   # (未移植)
    ui/
      button.tsx
      card.tsx
      input.tsx
      checkbox.tsx
```

現状の構成のメリット：

- シンプルで、ファイルがどこにあるかすぐわかる
- このプロジェクトの規模（ページ数が少ない・各ページが独立している）には十分

現状の構成の潜在的な問題：

- `components/` の中に「ページそのもの（stopwatch.tsx など）」と「UIパーツ（ui/ 以下）」が混在している
- ページ数が増えてきたとき、`components/` が肥大化する

## ADR-014: React のバージョンについて

- 日付: 2024-09-20（初期構築時）
- 状態: 記録のみ（意思決定なし）

`npm create vite@latest` のテンプレートが React 19 を採用していたため、特段の検討なしにそのまま使用している。
バージョンアップが必要になった際は、その時点で改めて検討・記録する。

## ADR-013: ルーティングライブラリの検討 → React Router を採用

- 日付: 2024-09-20（初期構築時の判断）、2025-04-18（mywebtools 移行時に v7 へ更新）
- 状態: 決定済み

### 背景

React は UI ライブラリであり、**ルーティング（URLと表示コンポーネントの対応付け）は自前で解決する必要がある**。
このプロジェクトでは複数のツールページを `/stopwatch`・`/timer` などの独立した URL で管理したいため、ルーティングライブラリの選定が必要になった。

選択肢：

- **React Router**
  - React 向けルーティングライブラリの事実上のデファクトスタンダード
  - URL とコンポーネントの対応付け、ネストしたレイアウト構成（`<Outlet />`）、ページ間リンク（`<Link />`）などを提供する
  - 前身プロジェクト（`my-web-tools`）では当時の最新である v6 を採用した
  - その後 v7 がリリースされ、本プロジェクト（`mywebtools`）の構築時点では v7 が最新であったため v7 を採用している
  - v6 → v7 は破壊的変更が少なく、基本的な使い方はほぼ同じ
- **TanStack Router**
  - 型安全性を強く重視した比較的新しいルーティングライブラリ
  - TypeScript との親和性が非常に高い
  - 採用実績・情報量はまだ React Router に及ばない
- **Next.js のファイルベースルーティング**
  - ファイルの配置がそのままURLに対応するルーティング方式
  - ただし Next.js はフレームワーク全体の採用を意味するため、Vite + React という本プロジェクトの構成とは合わない

### 決定内容

React Router を採用する。前身プロジェクト（`my-web-tools`）では v6、本プロジェクト（`mywebtools`）では v7 を使用している。

### 理由

- React のルーティングライブラリとしてのデファクトスタンダードであり、情報量・採用実績ともに十分
- このプロジェクトで必要な機能（URLとコンポーネントの対応付け・ネストしたレイアウト）を過不足なく満たしている
- `<Route>`・`<Link>`・`<Outlet />` といった基本的な概念を学ぶことが、フロントエンド開発の基礎力向上に直結する

### トレードオフ

- TanStack Router と比べると TypeScript の型安全性はやや弱い（URLパラメータの型が自動補完されないなど）
- v6 → v7 で一部 API が変わっており、古い記事を参照すると混乱することがある

---

## ADR-012: 言語の検討 → TypeScript を採用

- 日付: 2024-09-20（初期構築時の判断）
- 状態: 決定済み

### 背景

- React プロジェクトの実装言語として TypeScript を使うかどうかを検討した
- 選択肢：
  - JavaScript（素のJS）
    - ブラウザがネイティブに解釈できる言語。ビルド不要で動かせる
    - 変数や関数の引数に型がないため、「どんな値が来るか」をコードだけから把握しにくい
    - 大規模になるほど、意図しない値が流れ込んでのバグや、リファクタリング時の影響範囲の把握が困難になる
  - TypeScript
    - Microsoft が開発した、JavaScript に型システムを追加した言語
    - `.ts` / `.tsx` ファイルとして書き、ビルド時に JavaScript に変換（トランスパイル）される
    - エディタ上でコンパイルエラーが事前に表示されるため、バグをコードを書いている段階で発見できる
    - 関数・コンポーネントの引数や戻り値に型が付くため、コードを読むだけで「何を渡すべきか」が分かる

### 決定内容

TypeScript を採用する。

### 理由

- React 開発における TypeScript 採用は現在のデファクトスタンダードであり、習得は必須
- エディタ（VS Code など）との連携により、コンパイルエラーや補完が即座に表示され、開発体験が向上する
- コンポーネントの Props に型を付けることで、「このコンポーネントに何を渡すべきか」がコードを読むだけで分かる
  - ドキュメントやコメントの代わりを型が担うことになり、可読性・保守性が上がる
- 型エラーはビルド時にも検出されるため、実行前に問題に気づける安全網になる

### トレードオフ

- 型定義の記述が増えるため、素の JavaScript に比べてコード量が増える
- TypeScript 固有の概念（型推論・ジェネリクス・`as`・`satisfies` など）の学習コストが発生する
- ビルドステップが必須になる（ただし Vite を採用済みのため、追加コストはほぼない）

## ADR-011: ビルドツールの検討 → Vite を採用

- 日付: 2024-09-20（初期構築時の判断）
- 状態: 決定済み

### 背景

- React + TypeScript のプロジェクトをブラウザで動かすには、ビルドツールが必要になる
  - TypeScript は、そのままではブラウザが解釈できないため、JavaScript に変換（トランスパイル）する必要がある
  - JSX（`<Button />` のような React 独自の記法）も同様に、変換が必要
  - また、複数ファイルに分割して書いたコードを1つにまとめる（バンドル）処理も必要になる
  - ビルドツールはこれらをまとめて担う
- 選択肢：
  - Vite
    - 比較的新しいビルドツール。開発サーバーの起動・HMR（後述）が非常に高速
    - 設定がシンプルで、ゼロコンフィグに近い状態で動かせる
    - React + TypeScript のテンプレートが公式に用意されており、`npm create vite@latest` 一発でプロジェクトを始められる
    - 内部的にはネイティブ ES モジュール（ブラウザが直接理解できるモジュール形式）を活用しており、開発時のバンドルを最小限に抑えることで速度を実現している
  - Create React App (CRA)
    - かつての React プロジェクト作成のデファクトスタンダード
    - 内部で webpack を使っており、起動・ビルドが遅い
    - 2023年頃から公式ドキュメントでの推奨が外れ、事実上メンテナンスモードに入っている
  - webpack（単体）
    - 歴史が長く、最も広く使われてきたバンドラー
    - 設定の自由度が高い反面、設定ファイルが複雑になりやすい
    - 大規模プロジェクトの実績は豊富だが、学習コストが高い
  - Turbopack
    - Vercel が開発中の次世代バンドラー。Next.js と統合される方向で進んでいる
    - まだ開発途上で、Next.js 以外での単独利用は実用的でない

### 決定内容

Vite を採用する。

### 理由

- CRA は事実上非推奨になっており、新規採用する理由がない
- webpack 単体は設定の複雑さに対して、このプロジェクトの規模では恩恵が薄い
- Vite は設定がシンプルで、React + TypeScript テンプレートがすぐに使える
- 開発時の起動・HMR（Hot Module Replacement：コードを変更したとき、ページ全体をリロードせず変更箇所だけをブラウザに即時反映する仕組み）が高速で、開発体験が良い
- フロントエンド界隈での採用が急速に広がっており、学んでおく価値が高い

### トレードオフ

- webpack に比べると歴史が浅く、複雑なカスタマイズのノウハウはまだ少ない
- 開発時（Vite の開発サーバー）と本番ビルド時（Rollup によるバンドル）で内部の挙動が異なるため、まれに「開発では動いたが本番で動かない」という問題が起きうる

---

## ADR-010: スタイリング手法の検討 → Tailwind CSS を採用

- 日付: 2024-09-20（初期構築時の判断）
- 状態: 決定済み

### 背景

- React コンポーネントのスタイリング手法として何を使うか検討した
- 選択肢：
  - Tailwind CSS
    - ユーティリティクラスをHTMLに直接書いていくスタイル
    - クラス名とスタイルの定義が同じ場所に書かれるため、ファイルを行き来しなくてよい
    - 設定の学習コストはあるが、慣れると記述量が少なくて済む
    - shadcn/ui と相性が良い
  - CSS Modules
    - コンポーネントごとにスコープされた CSS を書く方法
    - 通常のCSSの知識がそのまま使えるため、移行コストが低い
    - Vite でそのまま使える（追加設定不要）
  - SCSS（Sass）
    - CSS の拡張言語。変数・ネスト・ミックスインなどが使える
    - 大規模プロジェクトでの管理がしやすい
    - Vite でも設定可能だが、追加パッケージが必要
  - CSS-in-JS（styled-components, Emotion など）
    - JavaScript の中にCSSを書く手法
    - コンポーネントとスタイルを完全に一体化できる
    - ランタイムオーバーヘッドがある（パフォーマンスに影響が出うる）
    - TypeScript との相性が良い

### 決定内容

Tailwind CSS を採用する。

### 理由

- このプロジェクトで採用予定の shadcn/ui が Tailwind CSS を前提としており、セットで採用するのが自然
- フロントエンドのデファクトスタンダードになりつつあり、実務でも需要が高い
- ユーティリティファーストのアプローチは、コンポーネント単位で開発するReactと相性が良い
  - スタイルのスコープ問題（意図せず他コンポーネントに影響が出る）が起きにくい

### トレードオフ

- クラス名がHTMLに大量に並ぶため、慣れるまで読みにくい
- Tailwind 固有の書き方を覚える必要があり、初期の学習コストが発生する
- 既存のCSSの知識とは別の概念（ユーティリティクラス）を習得する必要がある

---

## ADR-009: UIコンポーネントライブラリの検討 → shadcn/ui を採用

- 日付: 2024-09-20（初期構築時の判断）
- 状態: 決定済み

### 背景

- ボタン・カード・入力欄など、共通UIコンポーネントをどう調達するか検討した
- 選択肢：
  - shadcn/ui
    - 「コンポーネントライブラリ」ではなく「コードをプロジェクトにコピーして使う」方式
    - インストール後はコードが自分のリポジトリに入るため、自由にカスタマイズできる
    - Tailwind CSS + Radix UI を組み合わせて実装されている
    - アクセシビリティ（a11y）の対応が Radix UI によって担保されている
  - MUI (Material UI)
    - Googleの Material Design に準拠したコンポーネント群
    - 歴史が長く、ドキュメントやコミュニティが充実している
    - デザインがMUI独自のものになりやすく、カスタマイズに一手間かかる
  - Chakra UI
    - アクセシビリティを重視したコンポーネントライブラリ
    - テーマのカスタマイズがしやすい
    - Tailwind CSS との二重管理が生じる
  - コンポーネントを自作する
    - 自由度は最大だが、アクセシビリティ対応・デザインの一貫性確保などを自前で行う必要がある
    - 学習コストが高く、工数がかかる

### 決定内容

shadcn/ui を採用する。

### 理由

- Tailwind CSS を採用（ADR-010）した時点で、shadcn/ui はもっとも相性の良い選択肢になる
- コードがリポジトリに入る方式のため、コンポーネントの中身を読んで学べる
  - ライブラリのブラックボックスの中を自分で追えるため、フロントエンドの学習に向いている
- Radix UI ベースのため、アクセシビリティ対応が最初から組み込まれている
  - WAI-ARIA などの知識がなくても、基礎的なアクセシビリティが確保される
- 近年フロントエンド界隈での採用例が急増しており、触っておく価値がある

### トレードオフ

- コンポーネントが自分のリポジトリに入るため、ライブラリ側のアップデートを自動で受け取れない
  - 更新する場合は手動での取り込みが必要になる
- MUI などと比べるとコミュニティの歴史が浅く、情報量がやや少ない（ただし急速に増えている）

---

## ADR-008: Git のブランチ戦略の検討 → main + feature ブランチを採用

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- 個人開発であるため main ブランチ一本でも運用できる
- ツール単位で開発が分かれる性質を考慮すると、feature ブランチの採用が適している

### 決定内容

- main + feature ブランチ戦略を採用する。

### 理由

- ツール1本の追加がPR1本に対応し、変更の意図がコミット履歴に残りやすい
- 自分の開発スキル向上のために、main + feature ブランチでの開発に慣れたい
- フロントエンドの開発フローの流れを掴む上でも適している

### トレードオフ

- main ブランチ一本に比べてブランチ操作の手間が増える
- 個人開発のため厳密に運用しなくてよい

---

## ADR-007: テストの導入を検討 → テストは後回しにする

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- フロントエンドのテストに不慣れであり、学習コストが高い。
- そもそもフロントエンドに不慣れである。
- 一方で「動くものを作る」ことを優先したい。
- フロントエンドのテストの種類:
  - ユニットテスト
    - 関数・コンポーネント単体が正しく動くかを確認する
    - ツール: Vitest（Vite と相性が良い）
  - インテグレーションテスト
    - 複数のコンポーネントが組み合わさったときの動作確認
    - ツール: Testing Library（React Testing Library）
  - E2Eテスト（End to End）
    - ブラウザを実際に動かして、ユーザー操作を再現する
    - ツール: Playwright, Cypress

### 決定内容

- 当面はテストなしで進める。
- 将来 Vitest + React Testing Library を導入することを検討している

### 理由

- テストの学習で躓くよりも、まず動くものを作ることを優先する
- Vite プロジェクトとの相性から、導入時は Vitest + React Testing Library が自然な選択になる

---

## ADR-006: リンター・フォーマッターの検討 → Biome を採用

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- コードの品質維持・スタイル統一のためにリンターとフォーマッターの導入を検討した。
- 役割の違い:
  - リンター → コードの「問題」を検出する（バグになりそうな書き方など）
  - フォーマッター → コードの「見た目」を統一する（インデント・改行など）
- 選択肢:
  - ESLint + Prettier
    - 現状のフロントエンドのデファクトスタンダード
    - 設定ファイルが2つ必要
    - 設定が競合することがある
  - Biome
    - 1つでリンターとフォーマッターの両方を担える
    - 高速
    - 設定がシンプル

### 決定内容

Biome を採用する。

### 理由

- ESLint + Prettier は、過去に設定の競合などのトラブルを経験していて、良い印象が無い
- ESLint + Prettier は、設定ファイルが2つ必要で構成が煩雑になる
- Biome は設定ファイルがシンプルになる
- Biome は学習コストもあまり高くなさそうで、導入が楽そう
- 根拠は薄いが、Biome が今後のスタンダードになりそう

### トレードオフ

- Biome は ESLint + Prettier に比べて情報量が少ない
- Biome はまだ枯れていない可能性
  - ただし ESLint + Prettier でも十分にトラブルが発生していたため、許容できるデメリットだと判断した

---

## ADR-005: パッケージマネージャーの検討 → npm

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- 選択肢：
  - npm → Node.js に標準添付。
  - pnpm → 高速・省ディスク。チーム開発や大規模で恩恵が出やすい。
  - yarn → 昔 npm が遅かった時代に普及。今は優位性が薄れた。
  - bun → 新興。速い。まだ枯れていない。
- npm と yarn は使用経験有り。ただし、どちらも十分に使い慣れているとは言えない
- pnpm は気になっていて、使ってみたい。

### 決定内容

- npm を使う

### 理由

- npm で十分
- yarn は使う理由が薄い
- pnpm は良さそう。ただし、今の段階では npm との差を実感しにくい
- まずは npm を使って、フロントエンド開発の流れを掴むべき

---

## ADR-004: Node.js のバージョン管理方法を検討 → mise

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- Node.js のバージョン管理方法として何を使うか
- 選択肢：
  - mise
  - asdf
  - nvm
  - n, nodenv, volta, ...
- 選択肢は非常にたくさんある
- 使い慣れているのは mise と nvm.
- asdf, mise は、複数言語のバージョン管理を一元化できる

### 決定内容

- mise を使う

### 理由

- mise を別のプロジェクトでも使用していて、使い慣れている
- mise の感触が、かなり良い
  - シンプルに導入・利用ができ、学習コストが低い
  - 機能もシンプル
  - これ一つで多数のツールのバージョンを一元管理できる

---

## ADR-003: カスタムドメイン採用の有無を検討

- 日付: 2025-04-18
- 状態: 決定済み

### 決定内容

- （少なくともしばらくの間は）カスタムドメインは採用しない

### 理由

- React.js を中心としてフロントエンドスキルの習得に集中するべき
- まずは基本機能の作成に集中するべき
- 現状だとカスタムドメインを採用する理由が、Vercel のカスタムドメイン設定の練習になる、くらいしかない

---

## ADR-002: Cloudflare Pages は将来別プロジェクトで試す

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- Cloudflare Pages は Vercel と同等の機能を持ち、無料枠が広い。
- スキル向上のために触っておきたいサービスの一つ。

### 決定内容

- 今回の mywebtools では採用しない。将来別プロジェクトで試す。

### 理由

- 今回はフロントエンドスキルの習得を優先するため、ツールの学習コストを分散させない
- まずは Vercel に十分に慣れたべき。そうして始めて Cloudflare Pages との比較ができるようになる

---

## ADR-001: デプロイ先の検討 → Vercel を採用

- 日付: 2025-04-18
- 状態: 決定済み

### 背景

- 静的サイトのホスティング先を検討した
- 検討した選択肢：
  - GitHub Pages
    - 良い点：無料；GitHub と一体なのでシンプル
    - 悪い点：React Router を使う場合、追加設定が必要（SPAのリダイレクト問題）；ビルド設定がやや面倒
    - 慣れている
  - Vercel
    - 良い点：設定がほぼゼロ；push したら自動デプロイ；SPAのルーティング問題を勝手に解決してくれる；プレビューデプロイ機能がある
    - 悪い点：外部サービスへの依存が増える；無料枠に制限あり（個人利用なら実質問題なし）
    - 多少使ったことがある。慣れたい。
  - Cloudflare Pages
    - Cloudflare のサービスの一つで、静的サイトのホスティングができる
    - Vercel の競合。Vercel とほぼ同等。
    - 無料枠が Vercel より広め。
    - 使ったことがない。使ってみたい。
- SPAのルーティング問題とは
  - /stopwatch などに直接アクセスしたときに 404 になる問題。
  - GitHub Pages はそのままでは対応していないので、404.html を使ったハックが必要になる。
  - Vercel はこれを自動で処理するので楽。

### 決定内容

- Vercel を採用する。
- Cloudflare Pages は将来別プロジェクトで試す（ADR-002 参照）

### 理由

- GitHub Pages を選ばない理由：
  - 既に慣れているので、技術的に得るものが少ない
  - 特定プラットフォームへの依存度を下げるため、GitHub に強く依存することを（無理のない範囲で）避けたい
- Vercel を選ぶ理由：
  - 設定がほぼゼロで、フロントエンド開発本体に集中できる
  - フロントエンド業界で標準的なサービスであり、触っておく必要がある
  - SPA のルーティング問題（直接URLアクセス時の404）を自動で解決してくれる
- Cloudflare Pages を選ばない理由：
  - Cloudflare Pages も同等の機能を持つが、今回は学習コストの分散を避けるため見送った
